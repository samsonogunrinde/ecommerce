export const HomeIcon = ({width, onClick}) =>  <svg width={width} onClick={onclick} fill="currentColor" viewBox="0 0 20 20"><path d="M10.707 2.293a1 1 0 00-1.414 01"></path></svg>

export const DownIcon = ({width, onClick}) =>  <svg width={width} onClick={onclick} fill="currentColor" viewBox="0 0 20 20"><path d="M10 18a8 8 0 100-16 8 8 0 000 1"></path></svg>

export const UpIcon = ({width, onClick}) =>  <svg width={width} onClick={onclick} fill="currentColor" viewBox="0 0 20 20"><path d="M10 18a8 8 0 100-16 8 8 0 000 16"></path></svg>

export const CartIcon = ({width, onClick}) =>  <svg width={width} onClick={onclick} fill="currentColor" viewBox="0 0 20 20"><path d="M3 1a1 1 0 000 2h1.22l.305 1.2"></path></svg>

export const TrashIcon = ({width, onClick}) =>  <svg width={width} onClick={onclick} fill="currentColor" viewBox="0 0 20 20"><path d="M9 2a1 1 0 00-.894.553L7.382 4"></path></svg>
